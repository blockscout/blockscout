interface:
  display_name: "Implement Plan"
  short_description: "Implement a phased plan file"
  default_prompt: "Use $implement-plan to implement a phased plan file one phase at a time; it commits each phase to git and starts many subagents."
policy:
  allow_implicit_invocation: false
