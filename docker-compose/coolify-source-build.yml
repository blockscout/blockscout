# Blockscout deployment for Coolify — backend built FROM SOURCE (this repo,
# docker/Dockerfile) instead of pulled from ghcr.io/blockscout/blockscout.
#
# Why: ghcr.io/blockscout/blockscout is stuck at v9.0.2 (no publish in ~1
# year). Things this project already relies on only exist from v10.0.0+ — e.g.
# the admin auth (API_SENSITIVE_ENDPOINTS_KEY) only accepts the key via the
# x-api-key header starting v10.0.0; on v9.0.2 it's query-string-only
# (?api_key=...). HEAD of this repo is v11.2.1 (see mix.exs).
#
# This is meant to run as a SEPARATE Coolify resource/instance from the one
# using coolify.yml — same architecture, but:
#   - backend is built (docker-compose/services/backend-source-build.yml)
#     instead of pulling a prebuilt image; everything else (frontend,
#     visualizer, sig-provider, stats) still pulls prebuilt images, unchanged.
#   - hostnames are NOT hardcoded (coolify.yml hardcodes *.metrixchain.com —
#     reusing those here would make Coolify fight the existing production
#     instance for the same domains). Set BACKEND_HOST / EXPLORER_HOST /
#     STATS_HOST / VISUALIZER_HOST as env vars on this Coolify resource before
#     first deploy — e.g. backend-v11.metrixchain.com, explorer-v11...
#   - DB/stats-db passwords below are placeholders for a fresh instance; if
#     this second instance is meant to serve the SAME data as production,
#     point DATABASE_URL (in envs/common-blockscout.env) at the existing DB
#     instead of standing up a new one.
#
# Once this proves out, promote it: rename to coolify.yml (replacing the
# prebuilt-image version) or merge the two, and this repo's default deploy
# path builds from source going forward.

services:
  redis-db:
    extends:
      file: ./services/redis.yml
      service: redis-db

  db:
    image: postgres:17
    shm_size: 256m
    restart: always
    container_name: 'db'
    command: postgres -c 'max_connections=200' -c 'client_connection_check_interval=60000'
    environment:
      - POSTGRES_DB=blockscout
      - POSTGRES_USER=blockscout
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:?set a fresh password in Coolify env vars}
    volumes:
      - blockscout-db-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U blockscout -d blockscout"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  backend:
    depends_on:
      - db
      - redis-db
    extends:
      file: ./services/backend-source-build.yml
      service: backend
    links:
      - db:database
    expose:
      - "4000"
    environment:
      - SERVICE_FQDN_BACKEND_4000=https://${BACKEND_HOST:?set BACKEND_HOST in Coolify env vars, e.g. backend-v11.metrixchain.com}
      - BLOCKSCOUT_HOST=${BACKEND_HOST}
      - BLOCKSCOUT_PROTOCOL=https
      - CHECK_ORIGIN=false

  frontend:
    depends_on:
      - backend
    extends:
      file: ./services/frontend.yml
      service: frontend
    expose:
      - "3000"
    environment:
      - SERVICE_FQDN_FRONTEND_3000=https://${EXPLORER_HOST:?set EXPLORER_HOST in Coolify env vars, e.g. explorer-v11.metrixchain.com}

  visualizer:
    extends:
      file: ./services/visualizer.yml
      service: visualizer
    expose:
      - "8050"
    environment:
      - SERVICE_FQDN_VISUALIZER_8050=https://${VISUALIZER_HOST:?set VISUALIZER_HOST in Coolify env vars, e.g. visualizer-v11.metrixchain.com}

  sig-provider:
    extends:
      file: ./services/sig-provider.yml
      service: sig-provider

  stats-db:
    image: postgres:17
    shm_size: 256m
    restart: always
    container_name: 'stats-db'
    command: postgres -c 'max_connections=200'
    environment:
      - POSTGRES_DB=stats
      - POSTGRES_USER=stats
      - POSTGRES_PASSWORD=${STATS_POSTGRES_PASSWORD:?set a fresh password in Coolify env vars}
    volumes:
      - stats-db-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U stats -d stats"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  stats:
    depends_on:
      - stats-db
      - backend
    extends:
      file: ./services/stats.yml
      service: stats
    expose:
      - "8050"
    environment:
      - SERVICE_FQDN_STATS_8050=https://${STATS_HOST:?set STATS_HOST in Coolify env vars, e.g. stats-v11.metrixchain.com}
      - STATS__BLOCKSCOUT_API_URL=http://backend:4000
      - STATS__SERVER__HTTP__CORS__ENABLED=true
      - STATS__SERVER__HTTP__CORS__ALLOWED_ORIGIN=https://${EXPLORER_HOST}

volumes:
  blockscout-db-data:
  stats-db-data:
