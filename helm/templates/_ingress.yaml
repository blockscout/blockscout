{{- define "quai-blockscout.ingress" }}
- host: prime.{{ include "quai-blockscout.envPrefix" . -}}quaiscan.io
  http:
    paths:
    - path: /
      pathType: Prefix
      backend:
        service:
          name: prime-{{ include "quai-blockscout.name" $ }}
          port:
            name: http
{{- end }}
