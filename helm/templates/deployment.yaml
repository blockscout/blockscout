{{- range $chain := .Values.quaiBlockscout.chains }}
{{ include "quai-blockscout.bs" $ | replace "prime" $chain.name | replace "Prime" ($chain.name | title) | replace "<INDEX>" $chain.index }}
---
{{- end }}
